# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Docs live in the knowledge library

Full documentation is an Obsidian vault at **`/home/jonasz/workflow/library`**.
**Pull from it — don't preload.** Start at `/home/jonasz/workflow/library/MAP.md`; open only what the
task needs. Scan every note's purpose: `grep -rh "^summary:" /home/jonasz/workflow/library --include=*.md`.

- **Efficiency & clean-code rules → `/home/jonasz/workflow/library/_meta/claude-efficiency.md`** (general, read it)
  + **`/home/jonasz/workflow/library/_meta/project-rules.md`** (workflow-specific: build gates, shared libs, MCP, deploy)
- Session protocol → `/home/jonasz/workflow/library/_meta/workflow.md`
  (general workflow files are symlinks into the `~/.ai` repo)

## Critical always-on rules (details in the library)

- (the few rules that must never be missed — keep this list short)
