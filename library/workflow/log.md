---
project: workflow
type: log
audience: both
updated: 2026-07-06
summary: Append-only activity log — every piece of work, one dated line, newest first.
---

# workflow — log

Every completed piece of work gets one line (newest first). Big features also live
on [[board]]; this is the full history.

## 2026-07-06
- tmux: session switching made **positional** — `M-1`..`M-6` → n-th session in `tmux ls` order via `run-shell` (the old name-based `-t 0..5` broke the moment a session got renamed; session `0` → `projects` today). Status-left now shows the matching keymap digit: `[<n>:name]` via a `#()` position lookup, with `status-left-length 40` (the default 10 was truncating `"[projects] "` → the missing space before the first window name). `prefix m` = session menu (`choose-tree -Zs`, replacing default mark-pane). Resurrect save verified to carry the renamed session.
- tmux: window-select slots extended to `M-q`..`M-i` (windows 0–7; kept `M-o`/`M-p` as splits rather than taking the full qwertyuiop row — user's call). Dropped the `prefix t` rename binding (falls back to default clock-mode; `:rename-window` still makes labels stick) and the `-c` suffix on Claude-marked window labels (colour alone is the marker now) — [[tmux-window-status]] updated.
- resurrect: interval save timer 15min → 5min (`OnUnitActiveSec`), daemon-reloaded + restarted, immediate save triggered — [[tmux-resurrect]] updated.
- tmux: bound `M-1`..`M-6` → `switch-client -t 0`..`5` (Alt+digit session switching; digit N → session N-1, assumes default numeric session names). Confirmed the keys were unbound by default. Chose plain Alt+digit over Alt+Ctrl+digit (latter needs `extended-keys` + terminal keyboard-protocol support; Alt+digit is universally reliable). Repurposed the `choose-tree` pickers into create shortcuts: `prefix w` = `new-window`, `prefix s` = `command-prompt` → `new-session -s`, and `unbind-key c` (freed the default new-window key).
- Added `packages/` for full machine reproduction: `sync.sh` regenerates manifests from the live system (`pacman -Qqen` → `pacman-native.txt` 181, `pacman -Qqem` minus `-debug` → `aur.txt` 36, `snap list` → `snap.txt` 8, flatpak → empty), `restore.sh` reinstalls on a fresh Arch box (pacman -Syu, bootstraps yay, AUR, snaps/flatpaks; idempotent via `--needed`), `README.md` documents maintenance (run sync + commit; optional pacman PostTransaction hook) + the new-machine sequence (clone → dotfiles/install.sh → packages/restore.sh) + caveats (multilib, AUR drift, qt5 foreign, snap runtimes). New-machine flow in [[_moc]] updated.
- Fixed the tmux Stop focus check → grey-while-viewing now works: `#{client_focused}` returns empty in tmux 3.6, so the check always fell through to green; switched to grepping `#{client_flags}` for `focused`. Committed in `~/.ai` (settings.json is now a symlink there — 4th-gardening change; LOCK protocol used). Noted the gotcha in [[tmux-window-status]].
- Made the tmux status bar a Claude read/unread dashboard → [[tmux-window-status]]. Window names now = cwd basename (**bold+underline** when current, dropped `#I:`/`*`); `prefix t` labels a window (sticks + resurrect-persisted); splits (`M-o`/`M-p`) inherit the pane cwd. Status bar auto-shows only for >1 window — fixed it vanishing on reload by removing the unconditional `status off` and re-evaluating via a top-level `if-shell` after the hooks (every source now lands correct). Added `@claude_state` per-window option: when a Claude session is present the whole label is coloured with a `-c` suffix — **grey** (working / read) on SessionStart/UserPromptSubmit/PreToolUse, **yellow** (unread: wants input) on Notification/PermissionRequest, **green** (unread: finished) on Stop (focus-aware: stays grey if you're already viewing the window), cleared on SessionEnd (7 Claude Code hooks in untracked `~/.claude/settings.json`). Focusing a window (`pane-focus-in` tmux hook) "reads" it → grey. Validated formats + reload + transitions on throwaway tmux sockets. Logged a suggestion in `~/.ai` to version-control settings.json (LOCK protocol; committed there).

## 2026-07-05
- Gardening (env session): recovered the forked memory from the pre-rename `~/workspace` planning session → [[dotfiles-approach]] (bare-repo rationale + tool survey); deleted the fork (`~/.claude/projects/-home-jonasz-workspace/memory/`) and the empty `~/workspace` husk.
- Vendored the customized `kanagawa.nvim` fork as a **git submodule** (`dotfiles/.config/nvim/vendor/kanagawa.nvim` @ 61803cf) so the theme's real patches live in the repo instead of depending on the GitHub fork surviving. lazy loads it via `dir=` (resolving the symlinked config back to the repo path); install.sh now skips `vendor/`; dropped the stale kanagawa lazy-lock entry. Verified headless: colorscheme loads from the submodule, no errors. Update path: pull inside the submodule, commit the bump.
- Dropped stale `[maintenance] repo = /home/jonasz/library` from `.gitconfig` (dead path from before the migration; per-machine state that shouldn't be committed). Verified tracked files carry no secrets (only `.ssh/config` host aliases + key *pointers*, no keys). Captured the **directory-level symlink tracking** idea as [[dir-symlink-tracking]] + a backlog card on [[board]] — whole-dir symlinks so new config files auto-join the repo (candidates: `~/.config/git/`, then nvim).
- Recovered a session that died mid-migration (before its commit): completed the loose ends. Brought the tmux-resurrect **systemd units** (`.service`+`.timer`) under `dotfiles/.config/systemd/user/` — they were live in `$HOME` but untracked, so a fresh `install.sh` would've lost the autosave fix; re-ran install.sh to symlink them (timer still firing). Fixed `Claude.md` → `CLAUDE.md` (case-sensitive FS meant Claude Code never auto-loaded it). Folded the root `tmux-resurrect-fix.md` into the vault as [[tmux-resurrect]] with frontmatter + a new-machine `enable` caveat. Made the repo's first commit.
- Migrated dotfiles from the bare repo (`~/.dotfiles` + `$HOME` work-tree) into `dotfiles/` here: 27 tracked files (nvim, tmux, alacritty, i3, polybar, picom, bash, scripts, …), live uncommitted edits included; `.Xauthority` deliberately dropped. Added idempotent `dotfiles/install.sh` (symlinks into `$HOME`, backs up originals) and ran it — originals in `~/.dotfiles-backup-20260705-112508`. Retargeted `dotfiles` alias + `dfupdate()` in `.bashrc` at this repo. Bare repo kept untouched as archive.
- Project bootstrapped from `~/.ai` (vault skeleton, _meta symlinks, memory wiring).
